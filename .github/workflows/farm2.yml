name: Farm 2

on:
  schedule:
    - cron: "2-59/5 * * * *"
  workflow_dispatch:

jobs:
  farm2:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: commit farm
        run: |
          echo $(date) >> farm2.txt
          git config user.name "frhndevweb"
          git config user.email "frhndevweb@gmail.com"
          git add .
          git commit -m "farm $RANDOM"
          git push
